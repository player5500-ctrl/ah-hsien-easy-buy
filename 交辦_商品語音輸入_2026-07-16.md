# 交辦：商品資料繁體中文語音輸入（2026-07-16）→ Codex

> 用法：把「A. 現場盤點事實」＋「B. 架構修正」貼在 Vanny 那份 18 節規格**最前面**一起交給 Codex；規格本文照用。完成後由 Vanny 走「C. 驗收清單」（Codex 自報「已測」不算驗收）。
> ⚠️ 本專案只存在於 `D:\cowork files\projects\14_阿賢Easy購管理系統\`（同步夾，無 C:\ 版）。Codex 在此直接改。

## A. 現場盤點事實（Claude 已查證，Codex 以此為準，勿重猜）
- **架構**：純 vanilla JS SPA（HTML5+CSS3+ES6），**無框架、無 package.json、無 build step、無 module（import/export）**。全域函式掛 window，HTML 用 `onclick=` 呼叫。外部庫只有 CDN：SheetJS、FontAwesome 6.4.0。
- **商品資料模型**（`app.js:21-26`）：`id`(string,主鍵如P001)、`name`(string 商品名稱)、`specs`(string 規格)、`price`(number)、`unit`(string)、`enabled`(bool)、`photo`(string 選填)。
- **表單**：新增與編輯**共用**同一 modal `product-modal` 與函式。開啟 `openProductModal(id)`（空=新增、有id=編輯且 prod-id 設 readOnly），儲存 `saveProduct()`。
- **欄位 input id**（語音要填的目標）：`prod-name`、`prod-specs`、`prod-price`(type=number)、`prod-unit`。填完只改 `.value`，仍走既有 `saveProduct()`。
- **保存**：純 localStorage，key `easygo_products` 等，統一 `saveStateToStorage()` 全量寫；UI 靠 `renderProducts()` 手動刷新。語音只填表單、**不碰 storage**。
- **UI/主色**：FontAwesome `<i class="fa-solid fa-...">`；主按鈕 `btn btn-primary`；橘 `--primary-orange:#FF7A00`（hover `#E56E00`）；米白背景、大圓角、柔和陰影既有。
- **驗證**：集中在 `saveProduct()`，原生 `alert()`——必填 `id/name/price/unit`、售價 `parseFloat` 後 `isNaN||<0` 擋下、編號重複擋下。
- **測試**：目前**完全沒有**測試框架/測試檔。

## B. 架構修正（覆蓋規格中與實況衝突的假設）
1. **不要引入 React/Vue/Hook/npm/import-export**。語音功能用「新增一支 `voice-input.js`，以 `<script>` 在 index.html 載入（在 app.js 前）」＋ app.js 內加全域函式的方式做，維持「開 index.html 即用」。
2. **純函式獨立可測**：`中文數字轉換`、`商品語句解析` 兩個純函式放 `voice-input.js`，不依賴 DOM。
3. **測試方式**（無框架，別硬套 jest）：新增一個 `test-voice.html`——載入 `voice-input.js`，用內嵌 script 跑斷言（規格第 16 節那些案例），結果印在頁面與 console（綠/紅）。這就是本專案的「單元測試」。
4. **「型別檢查/tsc」不適用**（純 JS 非 TS）：改用 JSDoc 型別註解；規格第 15 節「避免 any」理解為「用 JSDoc 標型別」。
5. **lint**：專案無 eslint 設定，可略；若 Codex 自備則跑。
6. **build**：無 build step，規格第 17 節「production build」對本專案＝「用瀏覽器實際開 index.html 跑一輪」。
7. **SSR 疑慮不存在**（純靜態），但仍要做瀏覽器能力檢測：`window.SpeechRecognition||window.webkitSpeechRecognition`，不支援就隱藏麥克風鈕、顯示降級訊息，手動輸入照常。
8. **同步夾檔案完整性**：app.js 約 78KB，改完務必驗「檔尾完整＋行數合理」（避免同步夾寫入截斷），別只確認新函式有加進去。

## C. Vanny 驗收清單（Codex 交回後你花 3 分鐘跑，全過才算完成）
瀏覽器直接開 index.html（Chrome 桌面），到「商品管理」：
- [ ] 手動新增商品：照舊可用、可存、重整仍在。
- [ ] 手動編輯商品：照舊可用。
- [ ] 售價欄麥克風說「一百九十元」→ 售價變 `190`，其他欄不變。
- [ ] 語音快速新增說「商品名稱手工蛋捲，規格原味十二入，售價兩百元，單位盒」→ 預覽正確拆成 名稱=手工蛋捲/規格含12入/售價=200/單位=盒；按「套用到表單」才填入，**沒有自動儲存**。
- [ ] 只說「商品名稱紅茶，售價六十元」→ 名稱=紅茶、售價=60，規格/單位留空並提醒補充，不自動建檔。
- [ ] 拒絕麥克風權限 → 顯示中文友善訊息，手動輸入照常。
- [ ] 關閉語音對話框 → 錄音立即停止（不背景錄音）。
- [ ] 打開 `test-voice.html` → 中文數字與語句解析測試全綠（含 兩百→200、一百九十→190、十二入→12入、三十顆裝→30顆裝）。
- [ ] 手機視窗寬度：語音對話框不溢出、按鈕好按。
- [ ] Excel 匯入/匯出、商品搜尋/排序、啟用停用、既有商品資料 全部未受影響。

## 狀態
待轉 Codex 執行 → 回來後 Vanny 走 C 清單 → 全過結案；任一項未過帶失敗項回 Codex（同一問題最多兩輪，見工作流 C-6）。

<!-- ✂ 檔尾哨兵｜本檔至此完整結束 -->
